#include "dock_thumbnails.h"

// alignment 1 equals bottom, alignment 0 means top to position the popup correctly
void dock_thumbnails::open(float posx, float posy, int alignment, const std::vector<int> &ids) {
    
}

